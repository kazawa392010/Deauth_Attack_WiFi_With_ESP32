#ifndef RSSI_SCANNER_H
#define RSSI_SCANNER_H

#include <Arduino.h>

// Display all networks with their distances
void ui_show_rssi_scanner();

// Display network list with distances (for debugging/info)
void ui_show_networks_with_distance();

#endif
